apiVersion: v1
kind: Service
metadata:
  name: waste-sumdb-cluster-ip
  namespace: waste-system
spec:
  type: ClusterIP
  selector:
    app: waste-sumdb
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
