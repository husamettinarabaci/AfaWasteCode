apiVersion: v1
kind: Service
metadata:
  name: waste-lmsui-cluster-ip
  namespace: waste-system
spec:
  type: ClusterIP
  selector:
    app: waste-lmsui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
