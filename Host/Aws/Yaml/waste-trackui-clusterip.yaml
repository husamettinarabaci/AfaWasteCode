apiVersion: v1
kind: Service
metadata:
  name: waste-trackui-cluster-ip
  namespace: waste-system
spec:
  type: ClusterIP
  selector:
    app: waste-trackui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
