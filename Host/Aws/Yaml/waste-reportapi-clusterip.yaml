apiVersion: v1
kind: Service
metadata:
  name: waste-reportapi-cluster-ip
  namespace: waste-system
spec:
  type: ClusterIP
  selector:
    app: waste-reportapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
