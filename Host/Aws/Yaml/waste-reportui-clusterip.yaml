apiVersion: v1
kind: Service
metadata:
  name: waste-reportui-cluster-ip
  namespace: waste-system
spec:
  type: ClusterIP
  selector:
    app: waste-reportui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
