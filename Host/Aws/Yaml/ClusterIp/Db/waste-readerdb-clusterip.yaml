apiVersion: v1
kind: Service
metadata:
  name: waste-readerdb-cluster-ip
spec:
  type: ClusterIP
  selector:
    app: waste-readerdb
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
