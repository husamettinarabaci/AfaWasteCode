apiVersion: v1
kind: Service
metadata:
  name: waste-listener-loadbalancer
  namespace: waste-system
spec:
  type: LoadBalancer
  selector:
    app: waste-listener
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
