apiVersion: apps/v1
kind: Deployment
metadata:
  name: waste-staticdb-deployment
  namespace: waste-system
  labels:
    app: waste-staticdb
    apptype: waste-dbapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: waste-staticdb
  template:
    metadata:
      labels:
        app: waste-staticdb
    spec:
      containers:
        - name: waste-staticdb
          image: postgres:13.4
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          args: ["-c", "shared_buffers=256MB", "-c", "max_connections=400"]
          envFrom:
            - configMapRef:
                name: waste-psqldb-config
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: waste-staticdb-vm
              subPath: staticdb
      volumes:
        - name: waste-staticdb-vm
          persistentVolumeClaim:
            claimName: waste-staticdb-pv-claim
