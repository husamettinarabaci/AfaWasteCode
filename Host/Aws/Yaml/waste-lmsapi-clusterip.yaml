apiVersion: v1
kind: Service
metadata:
  name: waste-lmsapi-cluster-ip
  namespace: waste-system
spec:
  type: ClusterIP
  selector:
    app: waste-lmsapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
