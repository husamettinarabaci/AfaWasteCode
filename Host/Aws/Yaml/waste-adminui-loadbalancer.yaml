apiVersion: v1
kind: Service
metadata:
  name: waste-adminui-loadbalancer
  namespace: waste-system
spec:
  type: LoadBalancer
  selector:
    app: waste-adminui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30082
