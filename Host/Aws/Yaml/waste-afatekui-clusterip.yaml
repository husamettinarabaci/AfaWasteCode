apiVersion: v1
kind: Service
metadata:
  name: waste-afatekui-cluster-ip
  namespace: waste-system
spec:
  type: ClusterIP
  selector:
    app: waste-afatekui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
