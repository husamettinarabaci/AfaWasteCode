apiVersion: v1
kind: Service
metadata:
  name: waste-adminapi-loadbalancer
  namespace: waste-system
spec:
  type: LoadBalancer
  selector:
    app: waste-adminapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
