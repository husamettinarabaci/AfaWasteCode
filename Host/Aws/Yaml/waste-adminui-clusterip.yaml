apiVersion: v1
kind: Service
metadata:
  name: waste-adminui-cluster-ip
  namespace: waste-system
spec:
  type: ClusterIP
  selector:
    app: waste-adminui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
