apiVersion: v1
kind: Service
metadata:
  name: waste-trackapi-cluster-ip
  namespace: waste-system
spec:
  type: ClusterIP
  selector:
    app: waste-trackapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
