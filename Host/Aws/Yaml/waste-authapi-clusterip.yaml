apiVersion: v1
kind: Service
metadata:
  name: waste-authapi-cluster-ip
  namespace: waste-system
spec:
  type: ClusterIP
  selector:
    app: waste-authapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
